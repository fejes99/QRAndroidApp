<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qrmenuapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QRMenuApp">
        <activity android:name=".Admin.ProductSelections"></activity>
        <activity android:name=".Admin.PSAdapter" />
        <activity android:name=".Admin.PCartAdapter" />
        <activity android:name=".Admin.CartsActivity" />
        <activity android:name=".Admin.CartActivity" />
        <activity android:name=".Admin.ParentCartAdapter" />
        <activity android:name=".Admin.ChildCartAdapter" />
        <activity android:name=".Admin.Orders" />
        <activity android:name=".User.EndActivity" />
        <activity android:name=".User.LastActivity" />
        <activity android:name=".User.CartActivity" />
        <activity android:name=".User.CartAdapter" />
        <activity android:name=".User.cartActivity" />
        <activity android:name=".User.ProductCatalog$ProductItem" />
        <activity android:name=".recyclerView" />
        <activity android:name=".Admin.Profile" />
        <activity android:name=".User.ProductCatalog" />
        <activity android:name=".User.CameraActivity" />
        <activity android:name=".Admin.Auth.PasswordActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.Auth.RegisterActivity" />
        <activity android:name=".Admin.Auth.LoginActivity" />
        <activity android:name=".Admin.Auth.EntryActivity" />
        <activity android:name=".Admin.AddProduct" />
        <activity android:name=".Admin.UpdateProduct" />
        <activity android:name=".Admin.Products" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>